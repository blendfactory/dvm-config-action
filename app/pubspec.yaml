name: extract
description: "CLI app to extract DVM configuration files."
version: 2.0.0

executables:
  extract: app

environment:
  sdk: ^3.2.6

dependencies:
  file: ^7.0.0
  json_annotation: ^4.8.1
  meta: ^1.11.0
  recase: ^4.1.0

dev_dependencies:
  build_runner: ^2.4.8
  json_serializable: ^6.7.1
  test: ^1.24.0
  yumemi_lints: ^1.5.0
